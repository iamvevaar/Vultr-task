[pytest]
# Only look for tests in the tests/ folder.
testpaths = tests
# Add the working dir (/app) to sys.path so `import app...` resolves.
pythonpath = .
# -v = verbose (show each test name and pass/fail).
addopts = -v
